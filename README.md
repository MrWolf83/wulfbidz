# wulfbidz# Initialize git (if not already done)
git init

# Add all your files
git add .

# Make your first commit
git commit -m "Initial commit"

# Copy the commands from GitHub page - they'll look like this:
git remote add origin https://github.com/YOUR-USERNAME/wulfbidz.git
git branch -M main
git push -u origin main
